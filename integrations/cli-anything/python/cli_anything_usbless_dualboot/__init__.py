"""CLI-Anything harness for usbless-dualboot.

An agent-native wrapper around the existing `usbless-dualboot` bash CLI. It
describes and delegates; it does not reimplement any phase.
"""

__version__ = "0.1.0"